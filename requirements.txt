# --- Motor OCR Principal (PaddleOCR) ---
# Se recomienda instalar para CPU de forma predeterminada.
# En Windows/Linux con arquitectura x64, la versión 2.6.2 es la más estable.
paddlepaddle==2.6.2
paddleocr==2.8.1

# --- OCR Secundario (Fallback) ---
# Utilizado cuando PaddleOCR falta o en entornos ligeros.
easyocr

# --- Procesamiento y Machine Learning ---
# CRÍTICO: numpy < 2.0.0 es necesario para la compatibilidad con PaddleOCR y modelos guardados.
numpy<2.0.0
opencv-python
scikit-learn==1.5.0
joblib
pillow

# --- Datos y Entrenamiento ---
# Necesario para descargar y preprocesar el dataset CORD de HuggingFace.
datasets

# --- Fixes de Compatibilidad ---
# urllib3 < 2 es necesario para evitar advertencias de compatibilidad con requests/paddle.
urllib3<2
